# Camera Pan/Tilt Controller Requirements
# For Raspberry Pi 3 B+ with SlushEngine Model X LT

# SlushEngine library (install from GitHub)
# git clone https://github.com/Roboteurs/slushengine.git
# cd slushengine && sudo python3 setup.py install

# Required Python packages
spidev>=3.5
smbus2>=0.4.0

# Standard library modules used (no install needed):
# - socket (TCP server on port 5002)
# - json (command parsing)
# - threading (async client handling)

# For development/testing without hardware
# The controller has a mock mode for testing
